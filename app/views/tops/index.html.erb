<h1 class="font-bold text-4xl text-blue-600 mb-6">タスク管理アプリ</h1>

<div id="task_list" class="p-4 border border-gray-300 rounded-lg shadow-md bg-gray-50">
  <%= turbo_frame_tag "task_list" do %>
    <p class="text-gray-600 text-center">タスク一覧を表示するには上のボタンを押してください。</p>
  <% end %>
</div>

<div class="flex justify-center mb-6">
  <%= button_to "タスク一覧を表示", tasks_path(format: :turbo_stream), method: :get,
    data: { turbo_frame: "task_list" },
    class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" %>
</div>